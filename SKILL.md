---
name: "SKR Cram-Night Builder"
description: "สร้างชุดบทเรียน + เฉลยแบบฝึกหัดจากชีทของครู สำหรับสอบในโรงเรียน (เคมี ชีวะ ฟิสิกส์ คณิต ม.ปลาย) เป็นหน้า HTML ที่เปิดจากไฟล์ได้ทุกเบราว์เซอร์ ภาษาไทย ศัพท์อังกฤษกำกับ วิชาวิทย์ต้องมีรูปประกอบทุกบท (ครอปจากสไลด์ครู/วาด SVG เอง) ทุกตัวเลขคำนวณด้วย Python ก่อนเขียน. Use when studying for a Thai school test from a teacher's PDF sheet (ชีท) or slides, building or updating lessons in ~/Documents/SKR/<Subject>/, pulling diagrams out of the teacher's slides into a lesson, unlocking the sheet's own ตัวอย่าง/แบบฝึกหัด with hints and worked solutions, or recording a graded paper's wrong topics."
---

# SKR — ชุดบทเรียนคืนก่อนสอบ

ครูบอกล่วงหน้า 3–4 วัน คุณลงมือคืนก่อนสอบ นั่นคือเงื่อนไขจริง ไม่ใช่ข้อบกพร่อง
สกิลนี้จึงถูกออกแบบให้ **เก่งที่สุดในช่วง 4 ชั่วโมงนั้น** ไม่ใช่ให้คุณเปลี่ยนนิสัย

**สิ่งที่สกิลนี้ไม่ทำ:** ไม่เตือน ไม่นัดหมาย ไม่แตะปฏิทิน ไม่มีคำสั่งทบทวนแยก
ไม่ยุ่งกับการบ้าน ไม่ออกโจทย์ใหม่ — โจทย์ในชีทของครูคือโจทย์ที่จะออกสอบ

## กฎเหล็ก (ห้ามละเมิด)

1. **ต้องล็อกสเปกก่อนเขียนไฟล์แรกเสมอ** ถาม 3 คำถาม (ด้านล่าง) แล้ว *พูดสเปกกลับให้ฟัง*
   ก่อนลงมือ ห้ามเดาเงียบ ๆ
2. **เรียงตาม high-yield เสมอ ไม่เรียงตามหน้าในชีท** ถ้าเวลาหมดกลางทาง สิ่งที่เสร็จแล้ว
   ต้องเป็นสิ่งที่สำคัญที่สุด — หยุดตรงไหนก็ต้องมีของที่ใช้ได้จริง
3. **เคมี/ฟิสิกส์/คณิต: ทุกตัวเลขต้องคำนวณด้วย Python ก่อนเขียนลงไฟล์** ผ่าน
   `verify_lib.py` และถ้าชีทมีเฉลย ต้องตรงกับชีท — เฉลยผิดที่เขียนอย่างมั่นใจ
   แย่กว่าไม่มีเฉลย เพราะมันสอนวิธีที่จะถูกทำซ้ำในห้องสอบ
   **ชีววิทยา: ทุกข้อความต้องอ้างหน้าในชีทได้ รวมทั้งตำแหน่งทุกตำแหน่ง**
   ([VERIFY.md](docs/VERIFY.md) · [WHERE.md](docs/WHERE.md))
4. **ห้ามออกโจทย์ใหม่** ใช้ ตย./แบบฝึกหัด ในชีทเท่านั้น หน้าที่คือ *ปลดล็อก* ของที่มีอยู่
   ด้วยใบ้ก่อน–เฉลยทีหลัง ไม่ใช่เพิ่มของใหม่ให้ทำ
5. **ทุกบทต้องมีบล็อก "คัดลงสมุด"** อยู่บนสุด — ผู้เรียนลอกลง GoodNotes ด้วยมือ
   ต้องบอกให้ชัดว่าอะไรคุ้มค่าที่จะลอก ไม่ใช่ให้ไล่อ่านทั้งหน้าเพื่อหาเอง
6. **ต้องผ่าน `preflight.py` ก่อนส่งมอบ** ข้อที่บล็อกคือความพังแบบเงียบ ๆ ทั้งนั้น
   (ภาษาไทยเพี้ยน · CSS ไม่โหลดบน Safari · ควิซไม่ทำงาน · ตัวเลขไม่ได้ตรวจ)
7. **ต้องรัน `results.py weak` ก่อนวางแผนทุกครั้ง** จุดอ่อนจากกระดาษที่ตรวจแล้ว
   ถ่วงน้ำหนักบทเรียนชุดใหม่โดยอัตโนมัติ ไม่ต้องรอให้ผู้ใช้สั่ง
8. **ภาษาไทยเป็นหลัก ศัพท์เทคนิคใส่อังกฤษกำกับ** ตรงกับที่ครูสอนและที่ชีทใช้
9. **วิชาวิทย์: ทุกบทต้องมีรูป ไม่ใช่มีก็ดี** ชีวะ ≥ 4 รูป · เคมี/ฟิสิกส์ ≥ 3 · คณิต ≥ 2
   ไดอะแกรมบนสไลด์คือรูปที่จะออกสอบ วาดแบบที่คนตรวจคาดว่าจะเห็น — บรรยายเป็น
   ตัวหนังสือแทนคือทิ้งของฟรี และหน้าที่เป็นตัวอักษรล้วนคือหน้าที่ถูกเลื่อนผ่าน
   ตอนตี 1 เอาจากสไลด์ก่อน (`figure.py pull/crop`) วาด SVG เองเมื่อชีทไม่มี
   โหลดจากเน็ตเป็นทางเลือกสุดท้ายและต้องลงที่มา ([FIGURES.md](docs/FIGURES.md))
10. **ชีววิทยา: ทุกกระบวนการต้องบอกว่าเกิดที่ไหน** ใส่บล็อก `.where` ใต้หัวข้อ
    ก่อนคำอธิบาย — อวัยวะ · ส่วน/เซลล์ · **สร้างที่ vs ออกฤทธิ์ที่** ข้อสอบชีวะ
    ถาม "เกิดที่ใด" บ่อยกว่าถามกลไก และตำแหน่งที่กระจายอยู่ในย่อหน้าอ่านแล้วดูดี
    แต่ตอบไม่ได้ · ต้องมีบรรทัดตำแหน่งใน "คัดลงสมุด" และควิซ `data-tag="ตำแหน่ง-…"`
    อย่างน้อยหนึ่งข้อ ([WHERE.md](docs/WHERE.md))

## Environment

| สิ่งของ | ตำแหน่ง |
|---|---|
| ชีท + บทเรียนทั้งหมด | `~/Documents/SKR/<Subject>/` — Chemistry · Biology · Physics · Math |
| assets กลาง (แก้ที่เดียว) | `~/.claude/skills/skr/assets/` → `install.sh` ก๊อปไปทุก workspace |
| scripts | `~/.claude/skills/skr/scripts/` |
| สไลด์ครู (ต้นทางรูป) | `~/Documents/SKR/<Subject>/Slides/` |
| รูปของบทเรียน | `<workspace>/lessons/fig/` — ต้องอยู่ใน `lessons/` เท่านั้น |
| Python | `python3` + `pymupdf` (อ่าน PDF · ตัดรูป) + `sympy` (ตรวจตัวเลข) |

เครื่องใหม่หรือของหาย: `bash ~/.claude/skills/skr/install.sh` (`--check` = ตรวจอย่างเดียว)

## 0. ล็อกสเปก (ห้ามข้าม)

ถามสามข้อนี้ แล้วพูดสเปกกลับให้ฟังก่อนเขียนไฟล์:

1. **ชีทไหน หน้าไหน** — ไฟล์ PDF และช่วงหน้าที่ครูบอกว่าออกสอบ
2. **สอบเมื่อไร** — พรุ่งนี้เช้า? อีก 3 วัน? กำหนดว่าจะทำได้ลึกแค่ไหน
3. **คืนนี้มีเวลากี่ชั่วโมง** — กำหนดจำนวนบทและความละเอียด

แล้วรัน `python3 ~/.claude/skills/skr/scripts/results.py weak --subject <s>`
ถ้ามีจุดอ่อนเดิม ให้บอกผู้ใช้ว่าจะถ่วงน้ำหนักไปทางไหน ก่อนเริ่ม

## 1. อ่านชีท

```bash
S=~/.claude/skills/skr
python3 $S/scripts/sheet.py probe  "ชีท.pdf"                 # มี text layer ไหม
python3 $S/scripts/sheet.py text   "ชีท.pdf" --pages 20-30    # ถ้ามี — เร็วมาก
python3 $S/scripts/sheet.py render "ชีท.pdf" --pages 1-6 --out pages   # ถ้าสแกน
```

`probe` ก่อนเสมอ การเดาผิดเสียเวลาที่คืนก่อนสอบไม่มีให้เสีย
(เคมี = มี text layer · ชีววิทยา = สแกนล้วน ต้องเรนเดอร์เป็นภาพแล้วดู)

**อ่านชีทเสร็จให้สำรวจรูปทันที ในรอบเดียวกัน** อย่ารอจนเขียนบทเสร็จแล้วค่อยหารูป
— ตอนนั้นบทถูกเขียนเป็นย่อหน้าไปแล้ว และการยัดรูปเข้าไปทีหลังได้บทที่มีรูปแปะ
ไม่ใช่บทที่สร้างรอบรูป

```bash
python3 $S/scripts/figure.py list "Slides/Digestion.pdf" --pages 1-20
```

ได้บัญชีว่ามีรูปอะไรอยู่หน้าไหนบ้าง เอาไปใช้ตอนแบ่งบทในขั้นถัดไป

## 2. วางแผน — Opus

**สิ่งที่ Opus ต้องทำเอง ห้ามมอบต่อ:**
- อ่านชีท ตัดสินใจว่าแบ่งกี่บท บทไหนก่อน (high-yield ก่อน — กฎ 2)
- ระบุ "กับดัก" ของชีท: จุดที่โจทย์ยากมักดักไว้
- ออกแบบว่าอะไรต้องคำนวณ แล้วเขียน `verify.py`
- **เขียนเฉลยของโจทย์ยากด้วยตัวเอง** — เฉลยผิดอันตรายกว่าไม่มีเฉลย (กฎ 3)
- เขียนใบ้ (`data-hint`) ให้ชี้ทางโดยไม่บอกคำตอบ
- **จ่ายรูปให้แต่ละบท: รูปไหน มาจากสไลด์หน้าไหน คำบรรยายพูดว่าอะไร** (กฎ 9)
  การเลือกว่ารูปไหนสอนอะไรคือการตัดสินใจเรื่องการสอน ไม่ใช่งานกลไก — Sonnet
  ครอปให้ได้ แต่เลือกให้ไม่ได้ · บทไหนชีทไม่มีรูป ให้ระบุว่าจะวาด SVG อะไร
- **ชีวะ: กรอก `.where` ของทุกกระบวนการเอง** (กฎ 10) อวัยวะ · ส่วน/เซลล์ ·
  สร้างที่ · ออกฤทธิ์ที่ — ช่องไหนที่ชีทไม่ได้บอก **ห้ามเดา** ทุกช่องคือข้อความ
  ที่ต้องอ้างหน้าในชีทได้ และรูปของหัวข้อนั้นต้องใช้ชื่อตำแหน่งคำเดียวกันเป๊ะ

พูดโครงบทให้ผู้ใช้ฟังก่อนเขียน HTML

## 3. ลงมือ — Sonnet

งานกลไกทั้งหมดมอบให้ subagent ที่ตั้ง `model: "sonnet"`:
เขียน HTML ตามโครงที่ Opus วางไว้ · **ครอป/ดึงรูปตามที่ Opus จ่ายไว้ ลง `lessons/fig/`** ·
รัน `build.py` · เรนเดอร์หน้าสแกน · รัน `verify.py` · แก้ CSS/ภาษาไทย ·
รัน `figure.py check` แล้ว `preflight.py`

ส่งโครงบท เนื้อหาที่ Opus ตัดสินใจแล้ว รายการรูปพร้อมหน้าและคำบรรยาย
[LESSON-FORMAT.md](docs/LESSON-FORMAT.md) และ [FIGURES.md](docs/FIGURES.md)
ไปให้ครบในคำสั่งเดียว — subagent ไม่เห็นบทสนทนานี้

## 4. โครงหน้าบทเรียน

ทุกบทมีสองชั้น (กฎ 5) — รายละเอียดใน [LESSON-FORMAT.md](docs/LESSON-FORMAT.md)

```html
<div class="copy"><span class="copy-head">คัดลงสมุด</span>
  <!-- 5-8 บรรทัดที่คุ้มค่าที่สุด: สูตร ขั้นตอน กับดัก -->
</div>
<!-- แล้วค่อยอธิบายยาวข้างล่าง สำหรับตอนที่ติด -->
```

แบบฝึกหัดของครูใส่เป็น `data-type="reveal"` พร้อม `data-hint` — ใบ้ก่อน เฉลยทีหลัง
ไม่นับคะแนน เพราะมันคือที่ที่คุณซื่อสัตย์กับตัวเองได้ (กฎ 4)

## 5. ตรวจก่อนส่ง

```bash
cd ~/Documents/SKR/<Subject>
python3 verify.py                                   # ตัวเลขทุกตัว (เคมี/ฟิสิกส์/คณิต)
python3 build.py                                    # ฝัง CSS/JS ลงทุกหน้า
python3 ~/.claude/skills/skr/scripts/figure.py check lessons/fig
python3 ~/.claude/skills/skr/scripts/preflight.py --subject chem
```

`preflight` บล็อก = ห้ามส่งมอบ ต้องแก้ก่อน · warning = บอกแล้วส่งได้
รูปที่ `src` ชี้ไปไม่มีไฟล์ หรืออ้าง `../` หรือ hotlink เว็บ = บล็อก
รูปไม่ถึงโควตา และชีวะที่ไม่มี `.where` = warning แต่ **ต้องบอกผู้ใช้ตรง ๆ
ว่าบทไหนขาด** ไม่ใช่ปล่อยผ่านเงียบ ๆ

ตอนจบต้องบอกผู้ใช้ด้วยว่า **หน้าไหนต้องใช้ตาดูเอง** (รูป สูตร เฉลย ตำแหน่งของชีวะ) —
และต้อง **เปิดดูรูปที่ครอปหรือโหลดมาเองอย่างน้อยหนึ่งครั้ง** ก่อนส่ง
ไม่มีสคริปต์ไหนบอกได้ว่าครอปติดรูปที่ถูกหรือรูปที่อยู่ข้าง ๆ

## 6. หลังสอบ — บรรทัดเดียว

เมื่อได้กระดาษที่ตรวจแล้วคืนมา:

```bash
python3 ~/.claude/skills/skr/scripts/results.py add --subject chem \
    --chapter "บทที่ 9 สมดุล" --wrong "ICE table, Kp/Kc" --score 14/20
```

เขียนลง `learning-records/` + `results.jsonl` แล้วครั้งหน้ากฎ 7 จะหยิบไปใช้เอง
ผู้ใช้ไม่ต้องจำอะไรนอกจากบรรทัดนี้ ([RESULTS.md](docs/RESULTS.md))

## เอกสาร

| ไฟล์ | เรื่อง |
|---|---|
| [WORKFLOW.md](docs/WORKFLOW.md) | ลำดับงานเต็มของหนึ่งคืน พร้อมจุดตัดสินใจ |
| [LESSON-FORMAT.md](docs/LESSON-FORMAT.md) | โครง HTML คลาสที่มี ควิซทั้งสามแบบ |
| [VERIFY.md](docs/VERIFY.md) | เขียน `verify.py` ยังไง กฎการอ้างอิงของชีววิทยา |
| [SHEETS.md](docs/SHEETS.md) | อ่าน PDF ทั้งแบบมี text layer และแบบสแกน |
| [FIGURES.md](docs/FIGURES.md) | โควตารูปต่อบท · ดึงรูปจากสไลด์ · วาด SVG เอง · ที่มา |
| [WHERE.md](docs/WHERE.md) | ชีวะ: บล็อก "เกิดที่ไหน" · สร้างที่ vs ออกฤทธิ์ที่ |
| [RESULTS.md](docs/RESULTS.md) | บันทึกผลสอบ และการถ่วงน้ำหนักอัตโนมัติ |
| [TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md) | กับดัก `file://` ภาษาไทยเพี้ยน ควิซไม่ทำงาน |
